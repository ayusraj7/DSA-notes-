#include<bits/stdc++.h>
using namespace std; 
class Animal{
    public:
    void speak()
    {
        cout<<"Speaking"<<endl;
    }
};
class Dog:public Animal{
    public:
    void speak(){
        cout<<"Barking"<<endl;
    }
};
int main()
{
    // Animal a;
    // a.speak();
    // Animal *a=new Animal();
    // a->speak();
    // Dog *d=new Dog();
    // d->speak();

    //upcasting  
    Animal *a= new Dog();
    a->speak();
}